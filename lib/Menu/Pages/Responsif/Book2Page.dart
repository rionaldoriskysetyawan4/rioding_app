import 'package:flutter/material.dart';

class Book2page extends StatelessWidget {
  const Book2page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: <Widget>[Text("INI Respoon")],
      ),
    );
  }
}
